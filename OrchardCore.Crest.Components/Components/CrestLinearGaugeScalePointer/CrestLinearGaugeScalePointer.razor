@using Crest.Components.Primitives.Rendering

@namespace Crest.Components.Primitives
@if (Scale != null)
{
    <LinearGaugePointer
        Position=@CurrentPosition
        Start=@Scale.CurrentStart
        Orientation=@Scale.Orientation
        TickPosition=@Scale.TickPosition
        Fill=@Fill
        Stroke=@Stroke
        StrokeWidth=@StrokeWidth
        Size=@Size
        PointerType=@PointerType
        Draggable=@Draggable
        IsRTL=@Scale.IsRTL
        OnDragStart=@HandleDragStart
    />
}
