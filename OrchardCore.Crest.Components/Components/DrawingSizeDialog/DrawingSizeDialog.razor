@using Crest.Components.Primitives
@inject DialogService DialogService

@namespace Crest.Components.Primitives.Spreadsheet
@* Keyboard resize for a selected image/chart - the non-drag pointer alternative (WCAG 2.5.7).
   The host supplies the current size and localized labels; OK returns the new Size. *@
<CrestStack Gap="1rem">
    <CrestStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="0.5rem">
        <CrestLabel Text=@WidthLabel Component="rz-drawing-width" style="min-width:6rem" />
        <CrestNumeric @bind-Value=@width TValue="double" Min="1" Name="rz-drawing-width" aria-label=@WidthLabel />
    </CrestStack>
    <CrestStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="0.5rem">
        <CrestLabel Text=@HeightLabel Component="rz-drawing-height" style="min-width:6rem" />
        <CrestNumeric @bind-Value=@height TValue="double" Min="1" Name="rz-drawing-height" aria-label=@HeightLabel />
    </CrestStack>
    <CrestStack Orientation="Orientation.Horizontal" JustifyContent="JustifyContent.End" Gap="0.5rem">
        <CrestButton Text=@CancelText Click=@(() => DialogService.Close(null)) Variant="Variant.Flat" ButtonStyle="ButtonStyle.Light" />
        <CrestButton Text=@OkText Click=@OnOk ButtonStyle="ButtonStyle.Primary" />
    </CrestStack>
</CrestStack>

@code {
    /// <summary>Represents the chosen drawing size in pixels.</summary>
    public sealed record Size(double Width, double Height);

    /// <summary>The current width in pixels.</summary>
    [Parameter] public double Width { get; set; }

    /// <summary>The current height in pixels.</summary>
    [Parameter] public double Height { get; set; }

    /// <summary>Localized label for the width input.</summary>
    [Parameter] public string? WidthLabel { get; set; }

    /// <summary>Localized label for the height input.</summary>
    [Parameter] public string? HeightLabel { get; set; }

    /// <summary>Localized confirm-button text.</summary>
    [Parameter] public string? OkText { get; set; }

    /// <summary>Localized cancel-button text.</summary>
    [Parameter] public string? CancelText { get; set; }

    private double width;
    private double height;

    protected override void OnInitialized()
    {
        width = Width;
        height = Height;
    }

    private void OnOk() => DialogService.Close(new Size(width, height));
}
